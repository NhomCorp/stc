<!DOCTYPE html>
<html lang="vi">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Deploy Sổ Thu Chi — stc.acdagency.net</title>
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Fira+Code:wght@400;500&family=Inter:wght@400;500;600;700&display=swap" rel="stylesheet">
  <style>
    :root {
      --bg-main: #0f172a;
      --bg-card: #1e293b;
      --bg-code: #0b1120;
      --border-color: #334155;
      --text-main: #f1f5f9;
      --text-muted: #94a3b8;
      --accent-primary: #38bdf8;
      --accent-success: #10b981;
      --accent-warning: #f59e0b;
      --accent-danger: #ef4444;
      --btn-bg: #334155;
      --btn-hover: #475569;
    }
    * { box-sizing: border-box; margin: 0; padding: 0; }
    body {
      font-family: 'Inter', -apple-system, BlinkMacSystemFont, sans-serif;
      background: var(--bg-main);
      color: var(--text-main);
      line-height: 1.6;
      padding: 2.5rem 1rem;
    }
    .container { max-width: 900px; margin: 0 auto; }
    .header {
      margin-bottom: 2rem;
      border-bottom: 1px solid var(--border-color);
      padding-bottom: 1.25rem;
    }
    .header h1 {
      font-size: 1.75rem;
      font-weight: 700;
      color: #fff;
      margin-bottom: 0.75rem;
    }
    .pipeline-badge {
      display: inline-flex;
      flex-wrap: wrap;
      gap: 8px;
      background: rgba(56, 189, 248, 0.1);
      border: 1px solid rgba(56, 189, 248, 0.25);
      padding: 6px 14px;
      border-radius: 9999px;
      font-size: 0.88rem;
      color: var(--accent-primary);
      font-weight: 500;
      margin-bottom: 0.75rem;
    }
    .pipeline-arrow { color: var(--text-muted); font-weight: 400; }
    .header-desc { color: var(--text-muted); font-size: 0.98rem; }
    .meta-grid {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(220px, 1fr));
      gap: 10px;
      margin: 1.25rem 0 0;
    }
    .meta-item {
      background: #14223d;
      border: 1px solid #1e3a8a;
      border-radius: 8px;
      padding: 10px 12px;
      font-size: 0.86rem;
    }
    .meta-item strong {
      display: block;
      color: var(--accent-primary);
      font-size: 0.75rem;
      text-transform: uppercase;
      letter-spacing: 0.04em;
      margin-bottom: 2px;
    }
    .meta-item code { font-family: 'Fira Code', monospace; color: #e2e8f0; }
    .step-card {
      background: var(--bg-card);
      border: 1px solid var(--border-color);
      border-radius: 12px;
      padding: 1.35rem 1.5rem;
      margin-bottom: 1.35rem;
    }
    .step-header {
      display: flex;
      align-items: center;
      gap: 12px;
      margin-bottom: 0.75rem;
    }
    .step-number {
      background: var(--accent-primary);
      color: #0f172a;
      width: 28px;
      height: 28px;
      display: flex;
      align-items: center;
      justify-content: center;
      border-radius: 50%;
      font-weight: 700;
      font-size: 0.9rem;
      flex-shrink: 0;
    }
    .step-title { font-size: 1.15rem; font-weight: 600; color: #fff; }
    p { color: #cbd5e1; margin-bottom: 0.65rem; font-size: 0.94rem; }
    ul { color: #cbd5e1; margin: 0 0 0.75rem 1.2rem; font-size: 0.92rem; }
    li { margin-bottom: 0.35rem; }
    .code-wrapper {
      position: relative;
      background: var(--bg-code);
      border: 1px solid var(--border-color);
      border-radius: 8px;
      margin: 0.6rem 0 1rem;
      overflow: hidden;
    }
    .code-header {
      display: flex;
      justify-content: space-between;
      align-items: center;
      background: #151e32;
      padding: 6px 14px;
      font-size: 0.8rem;
      color: var(--text-muted);
      border-bottom: 1px solid #1e293b;
    }
    pre {
      margin: 0;
      padding: 1rem 1.15rem;
      overflow-x: auto;
      font-family: 'Fira Code', monospace;
      font-size: 0.88rem;
      line-height: 1.5;
      color: #e2e8f0;
    }
    .copy-btn {
      display: inline-flex;
      align-items: center;
      gap: 6px;
      background: var(--btn-bg);
      color: #e2e8f0;
      border: 1px solid #475569;
      border-radius: 6px;
      padding: 4px 10px;
      font-size: 0.78rem;
      font-weight: 500;
      cursor: pointer;
    }
    .copy-btn:hover { background: var(--btn-hover); color: #fff; }
    .copy-btn.copied {
      background: #065f46;
      border-color: var(--accent-success);
      color: #34d399;
    }
    .badge {
      display: inline-block;
      padding: 2px 8px;
      border-radius: 999px;
      font-size: 0.75rem;
      font-weight: 600;
      vertical-align: middle;
      margin-left: 8px;
    }
    .badge-ok { background: rgba(16,185,129,0.15); color: #34d399; }
    .badge-warn { background: rgba(245,158,11,0.15); color: #fbbf24; }
    .badge-once { background: rgba(148,163,184,0.15); color: #94a3b8; }
    .highlight-badge {
      background: rgba(56, 189, 248, 0.15);
      color: var(--accent-primary);
      padding: 2px 6px;
      border-radius: 4px;
      font-family: 'Fira Code', monospace;
      font-size: 0.88em;
    }
    .note {
      background: rgba(245, 158, 11, 0.08);
      border-left: 3px solid var(--accent-warning);
      padding: 0.75rem 1rem;
      border-radius: 0 8px 8px 0;
      margin: 0.75rem 0 0.5rem;
      font-size: 0.9rem;
      color: #fde68a;
    }
    .ok-box {
      background: rgba(16, 185, 129, 0.08);
      border-left: 3px solid var(--accent-success);
      padding: 0.75rem 1rem;
      border-radius: 0 8px 8px 0;
      margin: 0.75rem 0 0.5rem;
      font-size: 0.9rem;
      color: #a7f3d0;
    }
    .quick-summary { background: #14223d; border: 1px solid #1e3a8a; }
    .quick-summary .step-number { background: #60a5fa; }
    .alert-box {
      border-radius: 8px;
      padding: 1rem 1.25rem;
      margin-top: 0.5rem;
      margin-bottom: 1.5rem;
    }
    .alert-danger {
      background: rgba(239, 68, 68, 0.1);
      border: 1px solid rgba(239, 68, 68, 0.2);
      border-left: 4px solid var(--accent-danger);
    }
    .alert-title {
      font-weight: 700;
      color: #f87171;
      margin-bottom: 0.5rem;
    }
    .toast {
      position: fixed;
      bottom: 24px;
      right: 24px;
      background: #10b981;
      color: white;
      padding: 10px 18px;
      border-radius: 8px;
      font-size: 0.9rem;
      font-weight: 500;
      opacity: 0;
      transform: translateY(10px);
      transition: all 0.25s ease-out;
      pointer-events: none;
      z-index: 1000;
    }
    .toast.show { opacity: 1; transform: translateY(0); }
    a { color: var(--accent-primary); }
    .updated {
      color: var(--text-muted);
      font-size: 0.85rem;
      margin-top: 0.5rem;
    }
  </style>
</head>
<body>
<div class="container">

  <div class="header">
    <h1>Deploy Sổ Thu Chi</h1>
    <div class="pipeline-badge">
      <span>Local push main</span><span class="pipeline-arrow">➔</span>
      <span>GitHub Actions build image</span><span class="pipeline-arrow">➔</span>
      <span>VPS docker-compose pull</span><span class="pipeline-arrow">➔</span>
      <span>VPS docker-compose up</span>
    </div>
    <p class="header-desc">
      Nguyên tắc chắc chắn: <strong>Không build trên VPS</strong>. Mọi thao tác build được đưa lên GitHub Actions. VPS chỉ việc kéo image đã hoàn thiện (GHCR) về và chạy để tiết kiệm tài nguyên.
      Repo mã nguồn: <span class="highlight-badge">git@github.com:NhomCorp/stc.git</span>.
    </p>
    <p class="updated">Cập nhật 19/09/2026 — Kiến trúc mới: Build trên GHCR, VPS pull và run.</p>
    <div class="meta-grid">
      <div class="meta-item"><strong>Domain</strong><code>stc.acdagency.net</code></div>
      <div class="meta-item"><strong>App SSH</strong><code>ssh stc-vps</code> → <code>stcadmin</code></div>
      <div class="meta-item"><strong>Sudo / Nginx SSH</strong><code>ssh acd-vps</code> → <code>acdadmin</code></div>
      <div class="meta-item"><strong>App path</strong><code>/home/stcadmin/app</code></div>
      <div class="meta-item"><strong>Branch</strong><code>main</code></div>
      <div class="meta-item"><strong>Containers</strong><code>stc-app</code>, <code>stc-postgres</code></div>
      <div class="meta-item"><strong>Port nội bộ</strong><code>127.0.0.1:3000</code></div>
      <div class="meta-item"><strong>RAM VPS</strong>~3.8 GiB (build Docker dễ treo)</div>
      <div class="meta-item"><strong>Compose</strong><code>docker-compose</code> v5.x</div>
    </div>
  </div>

  <!-- ĐÚC KẾT SỰ CỐ -->
  <section class="step-card">
    <div class="step-header">
      <div class="step-number">!</div>
      <h2 class="step-title">Đúc kết vấn đề đã gặp (18/09/2026)</h2>
    </div>
    <ul>
      <li><strong><code>docker compose</code> không chạy</strong> trên VPS (Docker 20.10, không có plugin Compose V2). Lệnh bị hiểu sai → lỗi <code>unknown shorthand flag: 'd'</code>. Phải dùng <code>docker-compose</code>.</li>
      <li><strong>Working tree VPS bẩn</strong> dù cùng commit với local: có <code>next.config.js</code> lạ (untracked) và <code>package-lock.json</code> bị sửa. File lạ có thể lọt vào image nếu build từ thư mục đó.</li>
      <li><strong>Build trong Docker treo ở <code>next build</code></strong>: log dừng ở <em>Creating an optimized production build...</em>, process <code>processChild.js</code> sleep, <code>TIME</code> (CPU time) không tăng ~2 phút. RAM ~3.8 GiB — build Next trong container dễ nghẽn.</li>
      <li><strong>Treo ≠ chậm</strong>: nếu còn tăng CPU time / %CPU thì tiếp tục đợi; chỉ huỷ khi im CPU ~2 phút.</li>
      <li><strong><code>.dockerignore</code> có <code>.next</code></strong>: không thể <code>COPY .next/standalone</code> từ context app. Plan B phải build image từ thư mục context tạm (copy artifact ra ngoài).</li>
      <li><strong>PowerShell + SSH</strong>: <code>$(...)</code> trong chuỗi kép bị PowerShell expand local → lệnh VPS hỏng. Tránh hoặc dùng script bash trên VPS / quoting cẩn thận. Health check local dùng <code>curl.exe</code> (không dùng alias <code>curl</code> = Invoke-WebRequest).</li>
    </ul>
    <div class="ok-box">
      Cách đã cứu được: tạm dừng build Docker → <strong>npm run build trên host</strong> (nhanh, ổn) → đóng image runner từ context tạm → <code>docker run</code> lại <code>stc-app</code> cùng network <code>app_default</code> với postgres. Public lên healthy.
    </div>
  </section>

  <!-- QUY TRÌNH CHẮC CHẮN -->
  <section class="step-card quick-summary">
    <div class="step-header">
      <div class="step-number">⚡</div>
      <h2 class="step-title">Quy trình chắc chắn nhất — mỗi lần deploy cập nhật</h2>
    </div>
    <p>
      Mục tiêu: Đưa cập nhật lên public mượt mà nhất. Không đụng Nginx/SSL. <strong>Luôn giữ <code>.env</code> và database</strong>.
    </p>

    <p><strong>0 — Phạm vi</strong></p>
    <ul>
      <li>Dự án: Sổ Thu Chi · VPS Path: <code>/home/stcadmin/app</code></li>
      <li>Service: <code>stc-app</code> · Port nội bộ: <code>3000</code></li>
    </ul>

    <p><strong>1 — Local: Đẩy code mới lên GitHub</strong></p>
    <div class="code-wrapper">
      <div class="code-header">
        <span>PowerShell — local</span>
        <button class="copy-btn" onclick="copyCode(this)">Copy</button>
      </div>
      <pre><code>git add .
git commit -m "mô tả thay đổi"
git push origin main</code></pre>
    </div>
    <div class="note">
      Sau lệnh này, vào <a href="https://github.com/NhomCorp/stc/actions" target="_blank" style="color:var(--accent-primary)">GitHub Actions</a> chờ pipeline báo màu xanh (thành công) thì mới tiếp tục bước 2.
    </div>

    <p><strong>2 — VPS: Pull Image & Restart Container</strong></p>
    <div class="code-wrapper">
      <div class="code-header">
        <span>PowerShell — Update VPS</span>
        <button class="copy-btn" onclick="copyCode(this)">Copy</button>
      </div>
      <pre><code># Đẩy file docker-compose.yml mới lên (nếu có đổi)
scp .\docker-compose.yml stc-vps:/home/stcadmin/app/docker-compose.yml

# Chạy lệnh cập nhật
ssh stc-vps "cd /home/stcadmin/app && docker-compose pull app && docker rm -f stc-app && docker-compose up -d app"</code></pre>
    </div>
    <ul>
      <li><code>docker-compose pull app</code> — Lấy image đã hoàn thiện từ GitHub Packages về.</li>
      <li><code>docker rm -f stc-app</code> — Xóa container cũ (an toàn, không mất DB).</li>
      <li><code>docker-compose up -d app</code> — Chạy app bản mới ngay tức thì.</li>
    </ul>

    <p><strong>3 — Xác nhận chắc chắn đã chạy bình thường</strong></p>
    <div class="code-wrapper">
      <div class="code-header">
        <span>PowerShell — Đối chiếu health</span>
        <button class="copy-btn" onclick="copyCode(this)">Copy</button>
      </div>
      <pre><code># Xem log
ssh stc-vps "docker logs --tail=30 stc-app"

# Test API
ssh stc-vps "curl -s -o /dev/null -w '%{http_code}' http://127.0.0.1:3000/api/health"</code></pre>
    </div>
  </section>

  <!-- LẦN ĐẦU / ĐĂNG NHẬP -->
  <section class="step-card">
    <div class="step-header">
      <div class="step-number">!</div>
      <h2 class="step-title">Xử lý lỗi Access Denied (Đăng nhập GHCR)</h2>
    </div>
    <p>Dùng khi VPS báo lỗi không có quyền pull image <code>ghcr.io/nhomcorp/stc-app</code>.</p>
    <div class="code-wrapper">
      <div class="code-header">
        <span>VPS — Xác thực Registry</span>
        <button class="copy-btn" onclick="copyCode(this)">Copy</button>
      </div>
      <pre><code>ssh stc-vps "docker login ghcr.io -u <Tài_Khoản_GitHub>"
# Paste Personal Access Token (PAT) có quyền read:packages vào phần password</code></pre>
    </div>
  </section>

  <!-- One-time -->
  <section class="step-card">
    <div class="step-header">
      <div class="step-number">✓</div>
      <h2 class="step-title">Gắn git / Nginx / SSL <span class="badge badge-ok">ĐÃ CÓ</span></h2>
    </div>
    <div class="ok-box">
      Không cần chạy lại khi deploy thường ngày. DNS A <code>stc</code> → <code>103.180.135.145</code>.
      Nginx site <code>so-thu-chi</code> proxy → <code>127.0.0.1:3000</code>. SSL Certbot đã cấp cho domain.
      Sửa Nginx: <code>ssh acd-vps</code> (sudo).
    </div>
  </section>

  <div class="alert-box alert-danger">
    <div class="alert-title">Không chạy trên production</div>
    <div class="code-wrapper" style="margin-bottom:0;border-color:rgba(239,68,68,0.4)">
      <pre style="color:#fca5a5"><code>docker-compose down -v
git reset --hard          # thiếu origin/main — nguy hiểm / mơ hồ
git clean -fd             # không -e .env → có thể xoá nhầm nếu .env bị track
docker volume prune
docker system prune
docker stop $(docker ps -q)
pm2 restart all</code></pre>
    </div>
    <p style="color:#fca5a5;margin:0.75rem 0 0;font-size:0.9rem">
      Ngoại lệ an toàn (đã ghi ở quy trình): chỉ <code>git reset --hard origin/main</code> sau <code>git fetch</code>, trong <code>/home/stcadmin/app</code>, và luôn có <code>test -f .env</code> + <code>git clean ... -e .env</code>.
    </p>
  </div>

</div>

<div id="toast" class="toast">Đã sao chép!</div>
<script>
  function copyCode(btn) {
    const code = btn.closest('.code-wrapper').querySelector('pre code').innerText;
    navigator.clipboard.writeText(code).then(() => {
      const original = btn.innerHTML;
      btn.classList.add('copied');
      btn.innerHTML = 'Đã copy!';
      const toast = document.getElementById('toast');
      toast.classList.add('show');
      setTimeout(() => {
        btn.classList.remove('copied');
        btn.innerHTML = original;
        toast.classList.remove('show');
      }, 1800);
    });
  }
</script>
</body>
</html>
