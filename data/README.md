# Export Log từ Google Sheets (đọc-only) để import vào PostgreSQL.
# File log_export.json / sheet_export.xlsx bị gitignore vì chứa dữ liệu tài chính.

# Cách tạo lại export (cần file sheet_export.xlsx):
#   python scripts/export_log_from_xlsx.py
