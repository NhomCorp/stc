# Archive — không deploy

`Code.legacy.txt` = file nguyên khối hệ thống cũ (tham khảo flow).

- **Không** copy vào Apps Script project đang chạy.
- **Không** đổi đuôi thành `.gs` trong repo deploy.
- Tra cứu logic cũ tại đây khi cần port sang modular (`0_`…`9_*.gs`).
